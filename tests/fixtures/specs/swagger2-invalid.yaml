swagger: "2.0"
info:
  title: Broken Spec
paths:
  /broken:
    get:
      responses:
        "200":
          description: missing schema type causes validation issues
          schema:
            type: thisIsNotAValidType
